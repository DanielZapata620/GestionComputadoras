<Window x:Class="Cliente.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:Cliente.Viewmodels"
        xmlns:local="clr-namespace:Cliente"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:ClienteViewmodel></viewmodels:ClienteViewmodel>
    </Window.DataContext>
    <Grid>
        <TextBox HorizontalAlignment="Center" Margin="0,174,0,0" TextWrapping="Wrap" Text="{Binding IpServidor}" VerticalAlignment="Top" Width="120" />
        <TextBox HorizontalAlignment="Center" Margin="0,229,0,0" TextWrapping="Wrap" Text="{Binding Compu.Identificador}" VerticalAlignment="Top" Width="120"/>
        <Button Content="Button" HorizontalAlignment="Left" Margin="340,280,0,0" VerticalAlignment="Top" Height="27" Width="110" Command="{Binding RegistrarCommand}"/>
        <Label Content="{Binding Error}"  HorizontalAlignment="Left" Margin="395,91,0,0" VerticalAlignment="Top"/>

    </Grid>
</Window>
