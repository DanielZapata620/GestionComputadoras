<Window x:Class="Cliente.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:Cliente.Views"
        xmlns:viewmodels="clr-namespace:Cliente.Viewmodels"
        xmlns:local="clr-namespace:Cliente"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800" Background="#1E1B1B" ResizeMode="NoResize">
    <Window.DataContext>
        <viewmodels:ClienteViewmodel></viewmodels:ClienteViewmodel>
    </Window.DataContext>

    <Grid>
        <ContentControl>

            <ContentControl.Style>
                <Style TargetType="ContentControl">

                    <Setter Property="Content">
                        <Setter.Value>
                            <views:RegistroComputadoraView/>
                        </Setter.Value>
                    </Setter>

                    <Style.Triggers>

                        <DataTrigger Binding="{Binding VistaActual}" Value="ServidorEncendido">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <views:ConexionView></views:ConexionView>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>

                        <DataTrigger Binding="{Binding VistaActual}" Value="ServidorApagado">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <views:DesconexionView></views:DesconexionView>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>

                        <DataTrigger Binding="{Binding VistaActual}" Value="Registrar">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <views:RegistroComputadoraView></views:RegistroComputadoraView>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>

                    </Style.Triggers>

                </Style>
            </ContentControl.Style>

        </ContentControl>

   
    </Grid>
</Window>
