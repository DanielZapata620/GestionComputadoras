<Page x:Class="Servidor.Views.PanelView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:views="clr-namespace:Servidor.Views"
      xmlns:local="clr-namespace:Servidor.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PanelView"  Background="#1E1B1B">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- BARRA SUPERIOR -->
        <Border Background="#5C4F4F"
                Grid.Row="0">

            <Grid Margin="25,0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="PANEL DE CONTROL"
                           FontSize="28"
                           Foreground="White"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"/>

                <!-- BOTON HISTORIAL -->
                <Button Grid.Column="1"
                        Height="40"
                        Width="130"
                        Background="#2E2525"
                        Foreground="White"
                        BorderThickness="0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Cursor="Hand">

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                        <TextBlock Text="📋"
                                   FontSize="16"
                                   Margin="0,0,6,0"/>

                        <TextBlock Text="HISTORIAL"
                                   FontSize="13"/>

                    </StackPanel>

                </Button>

            </Grid>

        </Border>


        <!-- CONTENIDO -->
        <Grid Grid.Row="1"
              Margin="40,25">

            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- CONTROLES SUPERIORES -->
            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="140"/>
                </Grid.ColumnDefinitions>

                <!-- SELECTOR LAB -->
                <ComboBox Background="#6A5C5C"
                          Foreground="White"
                          BorderThickness="0"
                          Height="30"
                          VerticalAlignment="Center">

                    <ComboBoxItem Content="LAB01"/>
                    <ComboBoxItem Content="LAB02"/>
                    <ComboBoxItem Content="LAB03"/>

                </ComboBox>

                <!-- BOTON REFRESCAR -->
                <Button Grid.Column="2"
                        Background="#6A5C5C"
                        Foreground="White"
                        BorderThickness="0"
                        Margin="10,0"
                        Cursor="Hand">

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                        <TextBlock Text="⟳"
                                   FontSize="14"
                                   Margin="0,0,6,0"/>
                        <TextBlock Text="REFRESCAR"/>

                    </StackPanel>

                </Button>

                <!-- BOTON INTERNET -->
                <Button Grid.Column="3"
                        Background="#6A5C5C"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                        <TextBlock Text="📶"
                                   Margin="0,0,6,0"/>

                        <TextBlock Text="INTERNET"/>

                    </StackPanel>

                </Button>

            </Grid>


            <!-- PANEL DE COMPUTADORAS -->
            <ScrollViewer Grid.Row="1"
              VerticalScrollBarVisibility="Auto">

                <ItemsControl ItemsSource="{Binding ListaComputadoras}">

                    <!-- Panel donde se acomodan -->
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <!-- Template de cada item -->
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <views:ComputadoraView></views:ComputadoraView>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>

            </ScrollViewer>

        </Grid>

    </Grid>
</Page>
